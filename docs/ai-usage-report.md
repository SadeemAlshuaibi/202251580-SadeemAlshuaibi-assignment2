# AI Usage Report

## AI Tools Used
- ChatGPT


##  How AI Was Used

### 1. Code Assistance
- Generated JavaScript functions for:
    - Theme toggle
    - Search filtering
    - Form validation

### 2. Debugging
- Helped identify issues such as:
    - Missing element IDs
    - Incorrect event listeners
- Assisted in fixing bugs in:
    - Quote generator
    - Form validation

### 3. Feature Enhancement
AI suggested adding:
- Live search filtering
- Theme persistence using localStorage

### 4. Documentation
- Suggested clearer explanations


## Demonstration of Understanding

### Theme Toggle
I understood that the theme toggle works by adding or removing a class (e.g., "light") on the `<body>` element.  
The selected theme is stored in `localStorage` so it persists after page reload.

### Search Feature
The search feature listens to user input and filters project elements dynamically using JavaScript.  
I implemented real-time filtering and ensured that a message appears when no results are found.

### Form Validation
I used conditional statements to validate user input:
- Name must not be empty
- Email must follow a valid format
- Message must meet minimum length

Error messages are displayed using DOM manipulation, and a success message appears when inputs are valid.

### Quote Generator
The quote generator uses an array of quotes and selects one randomly when the user clicks a button.  
I ensured the function updates the content dynamically.


##  My Contribution
- Tested all features and fixed issues
- Adjusted logic to ensure correct functionality
- Designed layout and content independently



## What I Learned
- How JavaScript enables interactivity in web applications
- How localStorage works for saving user preferences
- How to validate forms and handle user input
- How to improve UX using feedback and animations
- How to debug and modify AI-generated code


##  Academic Integrity
AI was used only as a support tool.  
All code was reviewed, understood, and modified before submission.